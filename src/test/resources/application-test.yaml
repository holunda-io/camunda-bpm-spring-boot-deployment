camunda:
  bpm:
    deployment:
      enabled: true
      archives:
        - name: Default
          path: tenants/default
        - name: Foo
          tenant: foo
          path: tenants/foo
    generic-properties:
      properties:
        historyTimeToLive: P1D
